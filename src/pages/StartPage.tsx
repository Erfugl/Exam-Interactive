

StartPage.route = {
  path: '/',
  menuLabel: 'Start',
  index: 1
};

export default function StartPage() {
  return <section>
    <h3>A frame for every picture</h3>
    <h1>Wilde Frames</h1>

  </section>
}